<h2 class="text-muted">
  <% if @posts.any? %>
    <%= pluralize(@posts.count, 'resultado') %> encontrado:
  <% else %>
      Nenhum resultado encontrado.
  <% end %>
</h2>

<% @posts.each do |post| %>
  <%= render post %>
<% end %>
